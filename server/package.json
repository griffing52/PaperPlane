{
  "name": "paperplane-server",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "seed": "ts-node-dev --respawn=false scripts/seed.ts",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:ui": "vitest --ui"
  },
  "devDependencies": {
    "@types/commander": "^2.12.0",
    "commander": "^14.0.2"
  },
  "dependencies": {
    "zod": "^4.1.12"
  }
}
